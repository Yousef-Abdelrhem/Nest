export class CreateTodo {
  title: string;
}

// export class CreateTodo {
//     const newTodo = this.todo.map((todo) =>
//         todo.id === +id ? { ...todo, title } : todo,
//       );
//       this.todo = [...newTodo];4
//   }
  
